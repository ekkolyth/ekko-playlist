# API Configuration (optional - defaults usually work)
API_PORT=1337
WEB_PORT=3000

# REQUIRED: The public URL where your application is hosted
# Everything else derives from this URL
# Examples:
#   Development: http://localhost:3000
#   Production:  https://playlist.kenway.me
BETTER_AUTH_URL=http://localhost:3000

# REQUIRED: Generate a secure random secret
# Run: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-at-least-32-characters-long

# REQUIRED: PostgreSQL connection string
DB_URL=postgres://user:password@host:port/database

# OPTIONAL: Additional trusted origins for CORS (comma-separated)
# Only needed if you have additional domains that need access
# The browser extension ID is automatically allowed
TRUSTED_ORIGINS=

# OPTIONAL: Legacy CORS configuration
# Usually not needed - BETTER_AUTH_URL is automatically trusted
CORS_ALLOWED_ORIGINS=
